I asked Claude to review this project and its gave very useful advise.

So walkthrough the link and discover the Claude chats and his suggestions.

Then discover the @split_reference.py generated by Caude and use it as teamplate and adjust it if needed.

Review Claode's advise and if it possible to implement it, then do it.
Do not add any new features or change the core logic of the project.
Just improve the project based on claude suggestions and the split_reference.py script.  

Make sure that the project is still working as expected after the improvements.

And finally update the @to_do.md file with the list of changes made and the list of changes that are still needed.

---

### Changes Made (Based on Claude's Advice):
- Integrated the dual-layer architecture (Conceptual vs Procedural).
- Moved `split_references.py` to the root directory and adjusted it to run with defaults.
- Added a `SKIP_HEADINGS` filter to `split_references.py` to aggressively discard garbage sections (Table of Contents, Acknowledgements, etc.).
- Generated the clean `references/` directory, containing mechanically split, verbatim Docling markdown files without the cruft.
- Updated `README.md` to document the new `references/` layer.
- Created and successfully ran `add_background_links.py` to automatically append `## Background` sections to all 31 `SKILL.md` files, dynamically linking them directly to their conceptual counterparts in `references/`.

### Changes Still Needed (Future Work):
- Conduct a human-review sampling of the `_index.md` and `SKILL.md` outputs.
- Create a test suite to validate the skill extraction pipeline.
